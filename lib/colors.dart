import 'package:flutter/material.dart';

// This class holds all the color constants for the app,
// making it easy to manage the theme from one place.
class AppColors {
  static const Color primary = Color(0xFF276B16);
  static const Color backgroundLight = Color(0xFFF6F8F6);
  static const Color textLight = Color(0xFF152111);
  static const Color textMutedLight = Color(0xFF61725B);
  static const Color borderLight = Color(0xFFE1E4E1);
  static const Color textOnPrimary = Color(0xFFFFFFFF);
}
